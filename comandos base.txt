*Para criar o maven (Servidor)*
E qual o tipo que eu quero utilizar

mvn archetype:generate -DgroupId=com.ejcar -DartifactId=oficina -DarchetypeArtifactId=maven-archetype-quickstart

*Adicionar o id do grupo da aplicação*
-DgroupId=com.ejcar

*o id do meu projeto*
-DartifactId=oficina

mvn compile *para compilar devemos estar dentro da pasta*
mvn clean *limpar a pasta do projeto compilado a TARGET*

*comandos do commit *
echo "# estrela" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mltomacheski/estrela.git
git push -u origin main


CLASSES

atributos 
construtor padrao 
construtor uso 
get e set 
métodos toString 
método equal 
método hashCode
TUDO EDENTADO 
